# 2026-04-21 Sync Record

## Branch

`dev`

## Purpose

This branch is the BaseClaw shared layer. It sits between the upstream mirror branch and each variant branch.

Use this branch for:

- BaseClaw-wide branding
- shared scripts and tooling
- shared infrastructure for all variants
- fixes that should benefit every downstream variant

Do not use this branch for a single variant's custom UI, assets, or product behavior.

## Source

- Merged from local `main`
- Local result on this branch: `cd79d9f`

## What Was Absorbed From Main

This sync brought the latest upstream `ClawX/main` updates into `dev`, including:

- `v0.3.10` release changes
- chat execution graph and thinking message fixes
- channel status UI polish and Dingtalk multi-account fixes
- onboarding flow simplification
- enterprise extension bridge and marketplace wiring
- OpenClaw `4.15` upgrade
- refreshed E2E and unit coverage

The merge required manual conflict resolution in:

- `package.json`
- `docs/sync-records/2026-04-16-upstream-sync.md`

## What Dev Owners Should Review First

- packaging scripts and extension bridge generation
- shared provider and channel configuration flows
- chat rendering and execution graph behavior
- setup/onboarding flow after provider-step removal
- downstream variant branches that customize shared shell or navigation

## Next Step In The Branch Chain

After updating `dev`, each variant branch should merge `dev` and then review:

- branding files
- custom assets
- variant-specific UI
- variant-only docs

Reference:

- `docs/branch-strategy.md`
